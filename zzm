#include<stdio.h>
	int MAX(int a,int b,int c){
		int m;
		if(a>=b&&a>=c) m=a;
		else if(b>=a&&b>=c)m=b;
		else return m=c;
		return m;
	}
	int main(){
		int a,b,c;
		scanf("%d%d%d",&a,&b,&c);
		printf("max=%d",MAX(a,b,c));
		return 0;
		
	}
